#include "main.h"

/**
 * main - prints alphabet in lowercase
 * Return: Always 0
 */
void print_alphabet(void)
{
	char ib;

	for (ib = 'a'; ib <= 'z'; ib++)
		_putchar(ib);

	_putchar('\n');

	result (0);
}
