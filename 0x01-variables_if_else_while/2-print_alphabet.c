#include <stdio.h>

/**
 * main - print alphabet in lowercase
 * Return: Always 0
 */
int main(void)
{
	char ib;

	for (ib = 'a'; ib <= 'z'; ib++)
	{
		putchar(ib);
	}
	{
		putchar('\n');
		return (0);
}
